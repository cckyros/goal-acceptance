chore(release): bump to 0.1.2

Validated the Cordis plugin (`packages/goal-acceptance`) in the
DeepSeek Harness `goal-agent-preset` worktree:
- synced 0.1.1 source into `packages/goal/goal-acceptance` and
  `packages/goal/goal-acceptance-core`
- built with `tsc` and bundled with `tsdown` successfully
- all 78 tests passed (57 core + 18 Cordis plugin + 3 MCP)

Also adds the DSH plugin manifest from the previous commit
(`dsh.plugin.json`, `cordis.patch.yml`, package `dsh` field).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
